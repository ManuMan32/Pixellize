export type Language = "en" | "es";
export type ScreenState = "home" | "editor" | "galery" | "guide" | "options";
export enum TOOLS {
  BRUSH,
  ERASER,
  LINE,
  RECTANGLE,
  CIRCLE,
  POLYGON,
  BUCKET,
  SELECT
}